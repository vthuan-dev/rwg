package com.rwg.wallet.domain;

/**
 * Loại tham chiếu của một dòng ledger (V1.wallet_transactions.ref_type).
 */
public enum WalletRefType {
    BET, WIN, DEPOSIT, WITHDRAWAL, REFUND, BONUS, ADJUSTMENT, COMMISSION
}
